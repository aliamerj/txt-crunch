const std = @import("std");

// Placeholder for the library root file.
// We can include shared functionalities or utilities here.
pub fn greet() void {
    std.debug.print("Welcome to TxtCrunch!\n", .{});
}
